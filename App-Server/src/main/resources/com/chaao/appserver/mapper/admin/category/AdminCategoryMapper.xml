<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chaao.appserver.mapper.admin.category.AdminCategoryMapper">

    <select id="selectCategoryPage" resultType="entity.admin.category.Category">
        SELECT * FROM category
        WHERE deleted = 0
        <if test="dto.type != null">
            AND type = #{dto.type}
        </if>
        <if test="dto.name != null and dto.name != ''">
            AND name LIKE CONCAT('%',#{dto.name},'%')
        </if>
        ORDER BY sort ASC
        LIMIT #{dto.offset},#{dto.pageSize}
    </select>

    <select id="selectCategoryCount" resultType="java.lang.Long">
        SELECT COUNT(*) FROM category
        WHERE deleted = 0
        <if test="dto.type != null">
            AND type = #{dto.type}
        </if>
        <if test="dto.name != null and dto.name != ''">
            AND name LIKE CONCAT('%',#{dto.name},'%')
        </if>
    </select>

    <select id="selectEnableCategory" resultType="entity.admin.category.Category">
        SELECT * FROM category
        WHERE deleted = 0 AND status = 1
        <if test="type != null">
            AND type = #{type}
        </if>
        ORDER BY sort ASC
    </select>

    <insert id="insertCategory">
        INSERT INTO category
        (id,name,type,sort,status,create_time,create_user,update_user,deleted)
        VALUES
        (#{id},#{name},#{type},#{sort},#{status},#{createTime},#{createUser},#{updateUser},0)
    </insert>

    <update id="updateCategory">
        UPDATE category SET
        name=#{name},type=#{type},sort=#{sort},update_time=#{updateTime},update_user=#{updateUser}
        WHERE id=#{id} AND deleted=0
    </update>

    <update id="updateStatus">
        UPDATE category SET status=#{status},update_time=NOW() WHERE id=#{id} AND deleted=0
    </update>

    <update id="deleteCategory">
        UPDATE category SET deleted=1,update_time=NOW() WHERE id=#{id}
    </update>

    <select id="selectById" resultType="entity.admin.category.Category">
        SELECT * FROM category WHERE id=#{id} AND deleted=0
    </select>

</mapper>